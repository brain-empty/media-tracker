<section class="topmedia">
    <br>
<br>
<div> <!--list books-->
    <ul>
    <% books.forEach ( book => { %>
        <div class='media-box'><a href="/books/<%=book.id%>"><img src="<%= book.coverImagePath || "images/missing_movie.png"%>"><%=book.name%></a></div>
    <% }) %>
    </ul>
</div>

<p class="add-new">Can't find what you're looking for? <a href="books/new">Add a new book.</a></p>
<hr>
</section>